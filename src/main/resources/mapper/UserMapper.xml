<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lawmate.dao.UserDAO">

    <select id="login" resultType="com.lawmate.dto.UserDTO">
        SELECT
            user_id        AS userId,
            password,
            email,
            role,
            lawyer_status  AS lawyerStatus,
            license_file   AS licenseFile
        FROM users
        WHERE user_id = #{userId}
          AND password = #{password}
    </select>

    <select id="existsByUserId" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="save">
        INSERT INTO users (
            user_id,
            password,
            email,
            role,
            lawyer_status,
            license_file
        ) VALUES (
                     #{userId},
                     #{password},
                     #{email},
                     #{role},
                     #{lawyerStatus},
                     #{licenseFile}
                 )
    </insert>

    <select id="findPendingLawyers" resultType="com.lawmate.dto.UserDTO">
        SELECT
            user_id        AS userId,
            email,
            role,
            lawyer_status  AS lawyerStatus,
            license_file   AS licenseFile
        FROM users
        WHERE role = 'LAWYER'
          AND lawyer_status = 'PENDING'
    </select>

</mapper>