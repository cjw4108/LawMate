<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lawmate.dao.ScheduleMapper">

    <insert id="insertSchedule" parameterType="com.lawmate.dto.ScheduleRequestDto">
        INSERT INTO schedule (user_id, title, content, start_date, end_date)
        VALUES (#{userId}, #{title}, #{content}, #{startDate}, #{endDate})
    </insert>

    <select id="getLastInsertId" resultType="Long">
        SELECT LAST_INSERT_ID()
    </select>

    <select id="selectByUserId" parameterType="String" resultType="com.lawmate.dto.ScheduleResponseDto">
        SELECT * FROM schedule WHERE user_id = #{userId}
    </select>

</mapper>